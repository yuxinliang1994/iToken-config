spring:
  application:
    name: iToken-config
  boot:
    admin:
      client:
        url: http://${itoken.ip.admin}:${itoken.component.admin}
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/yuxinliang1994/iToken-config.git
          search-paths: respo
          username: 164553349@qq.com
          password: a8664362
  zipkin:
    base-url: http://${itoken.ip.zipkin}:${itoken.component.zipkin}

server:
  port: ${itoken.config.zipkin}
